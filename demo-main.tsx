import * as React from "react";
import * as ReactDOM from "react-dom";
import App from "./src/demo/App";

// Tương thích với React 16
ReactDOM.render(
    React.createElement(App),
    document.getElementById("root")
);

